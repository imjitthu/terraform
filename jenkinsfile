@Library('shared_library') _

    parameters {
    choice(name: 'action', choices: ['init', 'plan', 'validate', 'apply', 'destroy'], description: 'Pick any action')
               }
rsWebapp("${params.action}")